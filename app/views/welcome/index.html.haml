.introduction
  %h2 Welcome
  %p This is the home of Ruby Users of Minnesota &mdash; a fine group of Ruby enthusiasts from around the Twin Cities metro area, or pretty much wherever they feel like driving from. #{ link_to 'Our group meets on the last Monday of each month at 7 PM', '#meeting-info', :class => 'scroll' }

- if @events.any?
  .events
    %h2 Announcements
    - if !current_user.nil?
      %p= link_to "Add a New Event", new_event_path, :id => 'new_event_link'
    %ul
      = render :partial => "events/event", :collection => @events
