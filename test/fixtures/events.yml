first:
  headline: firstheadline
  body: first.body
  user: bob
  approved: true
  scheduled_time: <%=Time.now.to_s(:db)%>
another:
  headline: secondheadline
  body: secondbody
  user: existingbob
  approved: true
  scheduled_time: <%=Time.now.to_s(:db)%>
notapproved:
  headline: secondheadline
  body: secondbody
  user: existingbob
  scheduled_time: <%=Time.now.to_s(:db)%>
  approved: false
